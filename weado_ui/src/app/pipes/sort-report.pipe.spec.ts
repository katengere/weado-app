import { SortReportPipe } from './sort-report.pipe';

describe('SortReportPipe', () => {
  it('create an instance', () => {
    const pipe = new SortReportPipe();
    expect(pipe).toBeTruthy();
  });
});
